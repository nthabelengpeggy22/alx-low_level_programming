#include "main.h"

/**
 * _islower - checks for lowercase character
 * @c: character to be checked
 *
 * Return: 1 if c is lowercase, 0 if not
 */

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
