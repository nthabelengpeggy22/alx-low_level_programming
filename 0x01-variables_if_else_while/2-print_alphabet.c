#include <stdio.h>

/**
 * main -  prints the alphabet in lowercase
 * Return: Always 0 (Success)
 */

int main(void)
{
	char c;

	c = 'abcdefghijklmnopqrstuvwxyz';
	while
		(c = 'a' & c <= 'z')
	putchar(c);
	c++;

	putchar('\n');
	return (0);
}
