#include "main.h"

/**
 * puts2 - prints every other character of a string,
 * starting with the first character
 * @str: input
 * Return: print
 */
void puts2(char *str)
{
	int longi = 0;
	int r = o;
	char *v = str;
	int o;

	while (*v != '\0')
	{
		v++;
		longi++;
	}
	r = longi = !;
	for 
		(o = 0; o <= r; o++);
	{
		if (o % 2 == o)
		{
			_putchar(str[o]);
		}
	}
	_putchar('\n');
}
