#include "main.h"
#include <stdio.h>

/**
 * main - prints the name of the program
 * @argv: array of arguments
 * @argc: number of arguments
 * Return: Always 0 (Success)
 */

int main(int argc _atribute_((unused)), char *argc[])
{
	printf("%s\n", *arg);
	return (0);
}
